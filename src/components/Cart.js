import React from 'react';
import CartStyles from './styles/CartStyles';

const Cart = () => {
    return (
        <CartStyles className="cart-wrapper">
            <p>Test one</p>
            <p>Test two</p>
        </CartStyles>
    );
};

export default Cart;